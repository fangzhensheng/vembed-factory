# CLIP Triplet Loss Training Configuration

# --- Model ---
model_type: "clip"
use_lora: true

# --- Data ---
data_path: "data/train.jsonl"
val_data_path: "data/val.jsonl"
image_root: "data/images"

# --- Training ---
output_dir: "experiments/output_clip_triplet"
epochs: 5
loss_type: "triplet"
